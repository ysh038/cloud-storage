# FastAPI 웹 프레임워크
fastapi>=0.104.0

# ASGI 서버
uvicorn[standard]>=0.24.0

# MongoDB 관련
pymongo>=4.0.0

# 환경 변수 관리
python-dotenv>=0.19.0

# 파일 업로드 처리
python-multipart>=0.0.20

# 보안 관련
bcrypt>=4.0.0
PyJWT>=2.8.0

# 데이터 검증 (FastAPI 의존성이지만 명시적 사용)
pydantic>=2.0.0

# 타입 힌트 지원
typing-extensions>=4.8.0